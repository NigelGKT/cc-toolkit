# Log

> Chronological record of operations against this wiki. Grep-parseable — every entry begins with `## [YYYY-MM-DD HH:MM] <op> | <subject>` so `grep "^## \[" log.md | tail -10` works.

> Operations: `bootstrap`, `ingest`, `query`, `lint`, `manual`.
